further study:
sampling instead of every call 100 Hz sampling via perf_event
symbol cache
Python version offset detection
kernel + user stack merge
async-safe unwinding

libs:
Parca Agent 100 Hz sampling via perf_event
PySpy
PyPerf
